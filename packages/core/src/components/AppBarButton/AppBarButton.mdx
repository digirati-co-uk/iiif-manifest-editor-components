---
name: App Bar Button
menu: 05. Layout
---

import { Playground, Props } from 'docz';
import AppBarButton from './AppBarButton';
import { AppBar, Toolbar } from '@material-ui/core';
import { Add } from '@material-ui/icons';



# App Bar Button

Due to the different opinions about how an appbar button should look like, this component has been created to cater for everyone.

<Props of={AppBarButton.Naked} />


<Playground>
  <AppBar position="static">
    <Toolbar>
      <AppBarButton
        text="icon-and-tooltip"
        icon={<Add />}
        variant="icon-and-tooltip"
      />
      <AppBarButton
        text="icon-and-label"
        icon={<Add />}
        variant="icon-and-label"
      />
    </Toolbar>
  </AppBar>
</Playground>
