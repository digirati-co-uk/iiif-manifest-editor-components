import EditorReducer from './editor';

describe('EditorReducer', () => {
  describe('UPDATE_CONFIGURATION', () => {
    it('updates the editor configuration', () => {
      console.log('test');
    });
  });

  describe('LOAD_CONFIGURATION', () => {
    it('loads the editor configuration to the state', () => {
      console.log('test');
    });
  });
});
